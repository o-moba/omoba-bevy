# Verification findings and resolutions

- Initial server compilation: a missing test backend field and incorrect test receiver method were fixed before the passing server/shared suite.
- The first server suite exposed unsolicited Social packets reaching legacy framed clients. Explicit scoped Subscribe negotiation now gates social delivery; the unchanged real-UDP Release rematch test passes.
- Initial client compilation exposed missing test imports, an AssetLoader TypePath derive and a QA team enum mismatch. Fixed; native keyboard QA now uses KeyboardInput events rather than frame-local key flags.
- The sandboxed client run passed 303 tests but could not bind UDP sockets in seven existing tests (Operation not permitted). Re-running with local socket access passed all 310 tests.
- Review found an unsigned high request ID could poison an authenticated sender's receipt sequence. Authentication now precedes receipt mutation; the focused regression passes.
- Review found social framing sequences reused assembler keys after fast same-epoch rematches. Sequences remain monotonic for the epoch; a persistent-assembler interleaving regression covers the fix.
- Review found Practice omitted the account challenge identity guard. Admitted humans now retain that guard without creating a database allocation, including first authentication after guest admission.
- Launcher QA isolation now clears `_QA_OUTPUT` variables as well as prior QA switches. All 11 launcher tests pass.
- Review found asynchronous wheel configuration could show old pictures while sending new IDs. Render invalidation now includes all wheel IDs and resolved image handles/crops.
- Review found configured-but-unavailable PostgreSQL could unnecessarily block guest Practice chat. Routing now distinguishes an established authenticated session from a merely enabled database, while authenticated traffic remains signed.
- Native capture attempt 1 timed out before producing images. Attempt 2 added diagnostics and was stopped after they exposed two QA mistakes: Join had bypassed the local TeamSelection needed for client entity creation, and Bevy 0.18 stores native windows in WINIT_WINDOWS rather than the old non-send resource. The harness now commits the ordinary picker state before Join, reads the native window on the main thread, uses continuous QA updates, and requires both the OS focus event and native focus readback. Production input focus gates remain in force; no server events, players or match snapshots are fabricated.
- Native attempt 3 captured real Unicode chat and the wheel and received the reaction ACK, but failed the reaction node visibility check. Reaction/BOT nodes were being recreated each frame, and capture ran before visibility propagation. Rendering now preserves live node identity; capture measures after visibility propagation. QA also removes the picker overlay exactly as the ordinary Join/autojoin path does. Failed captures are retained separately from final evidence.
- Native attempt 4 passed all nine machine checks. Visual review still found desktop social controls over the HP/QWER cards and 3D bots using primitive fallback cubes. Desktop entry/status now sit below the minimap, with explicit HUD overlap regression/readback. Bots use eight existing class-appropriate free bundled models; catalog, files, free admission and unchanged 2D assignments have a regression. The final native pass follows these changes.
- Attempts 5 and 6 recorded genuine OS focus loss during the pre-send wheel gesture (all native/event/ECS focus observations false). Production correctly canceled the gesture; attempt 6's desktop captures already passed with final models and HUD positions. The opt-in harness now requests actual native focus at most three times, at least two seconds apart, and retries only canceled pre-send gestures after confirmed focus. It never replays an already sent reaction. Captures retain the focus request/event history; repeated refusal still fails.

- Final visual review found world-space BOT labels could draw over HP cards. Their layer is now below HUD nodes; all nine final captures were rebuilt and inspected afterward.
- Attempt 9 completed all desktop GPU captures and wrote a successful receipt, but the Python runner waited for delayed macOS/Bevy process shutdown and timed out before starting the phone cases. The runner now watches the fresh post-readback receipt, refuses nonempty output directories, and cleans up its owned processes. The next complete run passed all three modes without weakening focus, visibility or authoritative-server checks.
- Staging original Cargo logs reports four trailing blank lines under Git's whitespace checker. Raw output remains unchanged; the final whitespace command checks source and documentation while excluding the raw-artifact subtree.

Final integrated results and remaining platform/service limits are recorded in evidence.md and evidence.json. Earlier failure logs are retained locally as debugging history, not final acceptance evidence.
