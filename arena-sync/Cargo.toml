[package]
name = "arena-sync"
version.workspace = true
edition.workspace = true
description = "Pull Ekza Arena avatar cards from the chain into the omoba roster (VRM/GLB download + manifest merge)"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json.workspace = true
sha2 = "0.10"
base64 = "0.22"

[lints]
workspace = true
