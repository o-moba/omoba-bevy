# Forest and combat VFX foundation — 2026-09-17

Added `GameVfxPlugin` to the actual game client. Forest butterflies use bounded
analytic paths, animated textured wings and viewport culling. Confirmed combat
hits enter a reusable 128-slot particle pool after the existing replay/identity
filter. Magic, melee and ranged styles have separate presentation; impact profile
overrides remain the source of color, scale and lifetime. No gameplay, account,
payment or network protocol changes and no new production dependency.

CPU-updated transforms and shared meshes keep the initial implementation simple
for native desktop/mobile builds. This does not claim GPU simulation, measured
iPhone performance, or an uploaded TestFlight build. Old iPhone packages remain
intact. Task artifacts and native capture results are kept under
`.agent/tasks/GAME-VFX-2026-09-17/` locally.

Verification: client unit suite, bounded pool reuse/reset and render-mode tests,
locked offline native client build, and native live-server capture scenarios.
See task evidence for exact results and any remaining publication blockers.
