[package]
name = "harness"
version.workspace = true
edition.workspace = true
publish = false
description = "Headless gameplay test harness that drives the real UDP server with bot clients (no GPU, no human)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
